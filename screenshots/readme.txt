To use the screenshot function you just have to build a folder per profile named with its id.
Every jpeg in those folders will be used as a screenshot. Thumbs will be created automatically, so
you don't have to worry about them.